<odoo>
    <record id="view_vehicle_trip_form" model="ir.ui.view">
        <field name="name">vehicle.trip.form</field>
        <field name="model">vehicle.trip</field>
        <field name="arch" type="xml">
            <form string="Vehicle Trip">
                  <header>
                    <field name="state" widget="statusbar" statusbar_visible="draft,request,confirmed,approved"/>

                    <button name="action_request" type="object" string="request"
                            class="btn-primary"
                            invisible="state != 'draft'"/>

                    <button name="action_confirm" type="object" string="Confirm"
                            class="btn-primary"
                            invisible="state != 'request'"/>

                    <button name="action_approve" type="object" string="Approve"
                            class="btn-success"
                            invisible="state != 'confirmed'"/>
                </header>




                <sheet>
                    <group>
                        <group>
                            <field name="vehicle_type"/>
                            <field name="plate_number"/>
                            <field name="driver_name"/>
                        </group>

                        <group>
                            <field name="date"/>
                            <field name="start_location"/>
                            <field name="start_time"/>
                            <field name="odoo_meter"/>
                        </group>

                        <group>
                            <field name="destination"/>
                            <field name="end_date"/>
                            <field name="current_odoo_meter"/>
                            <field name="fuel_consumption"/>
                        </group>
                    </group>

                     <notebook>
                        <page string="Approval Info">
                            <group>
                                <group string="request">
                                    <field name="request_by"/>
                                    <field name="request_date"/>
                                </group>
                                <group string="Confirmed">
                                    <field name="confirmed_by"/>
                                    <field name="confirmed_date"/>
                                </group>
                                <group string="Approved">
                                    <field name="approved_by"/>
                                    <field name="approved_date"/>
                                </group>
                            </group>
                        </page>
                    </notebook> 
                </sheet>
            </form>
        </field>
    </record>

  
    <!-- Tree View -->
    <record id="view_vehicle_trip_tree" model="ir.ui.view">
        <field name="name">vehicle.trip.tree</field>
        <field name="model">vehicle.trip</field>
        <field name="arch" type="xml">
            <tree string="Vehicle Trips">
                <field name="date"/>
                <field name="vehicle_type"/>
                <field name="plate_number"/>
                <field name="driver_name"/>
                <field name="start_location"/>
                <field name="destination"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <!-- Action -->
    <record id="action_vehicle_trip" model="ir.actions.act_window">
        <field name="name">Vehicle Trips</field>
        <field name="res_model">vehicle.trip</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Menus -->
    <menuitem id="menu_vehicle_trip_root" name="vehicle Mngt" sequence="10"/>

    <menuitem id="menu_vehicle_trip_main"
              name="Trips"
              parent="menu_vehicle_trip_root"
              action="action_vehicle_trip"
              sequence="20"/>




</odoo>
